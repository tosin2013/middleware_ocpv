kind: Service
apiVersion: v1
metadata:
  name: {{ demo_name }}
  namespace: {{ demo_namespace }}
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  selector:
    kubevirt.io/domain: {{ demo_name }}
