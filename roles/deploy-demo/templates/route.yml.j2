kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ demo_name }}
  namespace: {{ demo_namespace }}
spec:
  to:
    kind: Service
    name: {{ demo_name }}
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None