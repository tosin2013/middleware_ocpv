kind: Service
apiVersion: v1
metadata:
  name: {{ lab_name }}
  namespace: {{ lab_namespace }}
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  selector:
    kubevirt.io/domain: {{ lab_name }}
